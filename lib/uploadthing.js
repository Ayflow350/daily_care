const { generateComponents } = require("@uploadthing/react");
const { OurFileRouter } = require("../src/api/uploadthing/core");

const { UploadButton, UploadDropzone, Uploader } = generateComponents();
